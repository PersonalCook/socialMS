apiVersion: v1
kind: Service
metadata:
  name: {{ include "social_service.fullname" . }}
  labels:
    {{- include "social_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "social_service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP

