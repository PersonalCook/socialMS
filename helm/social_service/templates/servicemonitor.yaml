apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: social-service-monitor
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - personalcook
  selector:
    matchLabels:
      app: social-service
  endpoints:
    - port: http             
      path: /metrics
      interval: 15s
      scheme: http
